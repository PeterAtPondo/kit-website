# macOS
.DS_Store

# Editors
.vscode/
.idea/

# Node (in case we add build tooling later)
node_modules/

# Local-only config
.env
.env.local
.vercel
.claude/

# Narration build intermediates (regenerated from posts by scripts/narration)
blog/*/narration.txt

# Video masters: the web-encoded .mp4 + poster .jpg are committed and served;
# the raw .mov screen-recordings (hundreds of MB) stay local only.
assets/video/*.mov
.feed-token
.env.diagnostic
